definitions:
  config:
    /hippo:configuration/hippo:translations/hippo:cms/validators:
      jcr:primaryType: hipposys:resourcebundles
      /en:
        jcr:primaryType: hipposys:resourcebundle
        email: Field doesn't match an email address
        escaped: Invalid character present
        non-empty: This field is required
        resource-required: You must upload a file
        summarySingle: One field is invalid.
        summaryMultiple: There are %d invalid fields.
