definitions:
  config:
    /topmost/upstream/exportFirst:
      jcr:primaryType: nt:unstructured
      existingPropertyWithLocalDef: [Don't call it a comeback, '... I''ve been here
          the whole time']
      existingPropertyWithAddInSecond: [value1]
      propertyWithResources:
        type: binary
        resource: [testResource.bin, testResource2.bin]
