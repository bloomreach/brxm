# This file is used by ClasspathConfigurationModelReaderTest and needs to be at the root of test-classes during test
groups:
- group: test-group
  projects:
  - project: test-project
    modules:
    - module: exportFirst
      after: [topmost,upstream]