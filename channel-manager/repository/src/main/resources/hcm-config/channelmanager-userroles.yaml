definitions:
  config:
    /hippo:configuration/hippo:userroles:
      /xm.channel.user:
        jcr:primaryType: hipposys:userrole
        hipposys:system: true
        hipposys:description: Required to view and use the Experience Manager (Channels) menu and functionality within the CMS
      /xm.channel.viewer:
        hipposys:userroles:
          operation: add
          value: [xm.channel.user]
