<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.onehippo.cms7</groupId>
    <artifactId>hippo-cms7-release</artifactId>
    <version>7.8.5</version>
  </parent>

  <name>Hippo Essentials</name>
  <description>Hippo Essentials</description>
  <groupId>org.onehippo.cms7.essentials</groupId>
  <artifactId>hippo-essentials</artifactId>
  <version>1.01.00</version>
  <packaging>pom</packaging>

  <properties>

    <!--***START temporary override of versions*** -->
    <hippo.cms.version>2.24.07</hippo.cms.version>
    <hippo.hst.version>2.26.08</hippo.hst.version>
    <hippo.repository.version>2.24.11</hippo.repository.version>
    <hippo.addon-channel-manager.version>2.24.08</hippo.addon-channel-manager.version>
    <!-- ***END temporary override of versions*** -->

    <jstl.version>1.1.2</jstl.version>
    <taglibs.version>1.1.2</taglibs.version>
    <commons.lang.version>2.6</commons.lang.version>
    <junit.version>4.11</junit.version>
    <easymock.version>3.1</easymock.version>
    <freemarker.version>2.3.18</freemarker.version>
    <filesync.override>false</filesync.override>
    <maven.plugins.filesync.version>1.0.0</maven.plugins.filesync.version>

    <cargo.tomcat.major.version>7</cargo.tomcat.major.version>
    <cargo.tomcat.full.version>7.0.42</cargo.tomcat.full.version>

    <!-- Tells Sonar to run the unit tests -->
    <sonar.dynamicAnalysis>true</sonar.dynamicAnalysis>
    <!-- Tells Sonar to use JaCoCo as the code coverage tool -->
    <sonar.java.coveragePlugin>jacoco</sonar.java.coveragePlugin>
    <sonar.jacoco.reportPath>${basedir}/target/target/jacoco.exec</sonar.jacoco.reportPath>

  </properties>
  <scm>
    <connection>scm:git:git@github.com:onehippo/essentials.git</connection>
    <url>scm:git:git@github.com:onehippo/essentials.git</url>
    <developerConnection>scm:git:git@github.com:onehippo/essentials.git</developerConnection>
    <tag>hippo-essentials-1.01.00</tag>
  </scm>
  <!-- PUBLISH TO PRIVATE REPO-->
  <distributionManagement>
    <repository>
      <id>hippo-maven2-private</id>
      <name>Hippo Private Maven 2</name>
      <url>https://maven.onehippo.com/content/repositories/private-releases/</url>
      <uniqueVersion>false</uniqueVersion>
    </repository>
    <snapshotRepository>
      <id>hippo-maven2-private-snapshots</id>
      <name>Hippo Private Maven 2 Snapshots</name>
      <url>https://maven.onehippo.com/content/repositories/private-snapshots/</url>
      <uniqueVersion>false</uniqueVersion>
    </snapshotRepository>
  </distributionManagement>


  <modules>
    <module>plugin-api</module>
    <module>dashboard-plugins</module>
    <module>plugin-dashboard</module>
  </modules>


  <repositories>
    <repository>
      <id>hippo-maven2-private</id>
      <name>Hippo maven 2 private repository.</name>
      <url>https://maven.onehippo.com/maven2-private</url>
    </repository>
    <repository>
      <id>hippo-maven2-private-snapshots</id>
      <name>Hippo maven 2 private repository snapshots</name>
      <url>http://maven.onehippo.com/maven2-private-snapshots</url>
    </repository>
    <!--<repository>
      <id>hippo</id>
      <name>Hippo maven 2 repository.</name>
      <url>http://maven.onehippo.com/maven2/</url>
    </repository>-->
    <repository>
      <id>hippo-forge</id>
      <name>Hippo Forge maven 2 repository.</name>
      <url>http://maven.onehippo.com/maven2-forge/</url>
      <snapshots>
        <enabled>false</enabled>
      </snapshots>
      <releases>
        <updatePolicy>never</updatePolicy>
      </releases>
      <layout>default</layout>
    </repository>
  </repositories>

  <dependencyManagement>
    <dependencies>

      <!-- other predefined runtime scope versioned dependencies -->
      <dependency>
        <groupId>javax.servlet</groupId>
        <artifactId>jstl</artifactId>
        <version>${jstl.version}</version>
        <scope>runtime</scope>
      </dependency>

      <dependency>
        <groupId>taglibs</groupId>
        <artifactId>standard</artifactId>
        <version>${taglibs.version}</version>
        <scope>runtime</scope>
      </dependency>

      <!-- other predefined compile scope versioned dependencies -->
      <dependency>
        <groupId>commons-lang</groupId>
        <artifactId>commons-lang</artifactId>
        <version>${commons.lang.version}</version>
      </dependency>

      <dependency>
        <groupId>org.freemarker</groupId>
        <artifactId>freemarker</artifactId>
        <version>${freemarker.version}</version>
      </dependency>

      <dependency>
        <groupId>junit</groupId>
        <artifactId>junit</artifactId>
        <version>${junit.version}</version>
        <scope>test</scope>
      </dependency>

      <dependency>
        <groupId>org.easymock</groupId>
        <artifactId>easymock</artifactId>
        <version>${easymock.version}</version>
        <scope>test</scope>
      </dependency>

    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>javax.servlet</groupId>
      <artifactId>servlet-api</artifactId>
      <version>2.5</version>
      <scope>provided</scope>
    </dependency>
  </dependencies>

  <build>
    <defaultGoal>package</defaultGoal>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <configuration>
            <target>1.7</target>
            <source>1.7</source>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>
    <plugins>
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>0.6.4.201312101107</version>
        <executions>
          <execution>
            <id>jacoco-initialize</id>
            <goals>
              <goal>prepare-agent</goal>
            </goals>
            <configuration>
              <excludes>
                <exclude>org.hippoecm.*</exclude>
              </excludes>
              <destFile>${basedir}/target/jacoco.exec</destFile>
            </configuration>
          </execution>
          <execution>
            <id>jacoco-site</id>
            <phase>verify</phase>
            <goals>
              <goal>report</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

</project>
