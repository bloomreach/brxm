define(['backbone'],
        function (Backbone) {

            var models = {};

            models.Dependency = Backbone.RelationalModel.extend({
            });

            return models;
        }
);
