definitions:
  config:
    /exportFirstExistingRoot:
      # should result in def disappearing from exportFirst
      .meta:delete: true
    /topmost/upstreamToDelete:
      # should result in new delete-def in exportFirst
      .meta:delete: true
    /topmost/upstream:
      # should result in new delete-def in exportFirst, removed defs in exportFirst and exportSecond, and removed exportSecond Source
      .meta:delete: true
