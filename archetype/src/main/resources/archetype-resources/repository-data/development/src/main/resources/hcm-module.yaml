group: 
  name: ${artifactId}
  after: hippo-cms
project: ${artifactId}
module: 
  name: ${artifactId}-repository-data-development
  after: [${artifactId}-repository-data-application, ${artifactId}-repository-data-webfiles]