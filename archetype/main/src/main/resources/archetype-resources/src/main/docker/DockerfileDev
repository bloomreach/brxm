FROM ${project.groupId}/${project.artifactId}:${project.version}

LABEL PROJECT=${project.artifactId}:dev-${project.version}

COPY --chown=${docker.container.uid}:${docker.container.uid} maven/webapps /usr/local/tomcat/webapps/
COPY --chown=${docker.container.uid}:${docker.container.uid} maven/autoreload /usr/local/tomcat/shared/lib
${docker.copy.dev.libs.instruction}
