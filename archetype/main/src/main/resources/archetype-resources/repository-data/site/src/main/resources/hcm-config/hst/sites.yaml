#set( $symbol_pound = '#' )
#set( $symbol_dollar = '$' )
#set( $symbol_escape = '\' )
#set($hyphen = '-')
#set($empty = '')
definitions:
  config:
    /hst:hst/hst:sites/${rootArtifactId.replace($hyphen,$empty)}:
      jcr:primaryType: hst:site
      hst:content: /content/documents/${rootArtifactId.replace($hyphen,$empty)}
