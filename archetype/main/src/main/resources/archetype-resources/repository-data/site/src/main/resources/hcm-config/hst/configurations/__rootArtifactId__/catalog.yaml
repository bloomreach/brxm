#set( $symbol_pound = '#' )
#set( $symbol_dollar = '$' )
#set( $symbol_escape = '\' )
#set($hyphen = '-')
#set($empty = '')
definitions:
  config:
    /hst:hst/hst:configurations/${rootArtifactId.replace($hyphen,$empty)}/hst:catalog:
      jcr:primaryType: hst:catalog
      /${rootArtifactId.replace($hyphen,$empty)}-catalog:
        jcr:primaryType: hst:containeritempackage
