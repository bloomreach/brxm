definitions:
  config:
    /hippo:configuration/hippo:translations/hippo:cms/validators:
      /zh:
        escaped: 出现无效字符
        resource-required: 必须上传一个文件
        non-empty: 一个强制性字符串输入字段为空
        jcr:primaryType: hipposys:resourcebundle
        email: 字段与电子邮件地址不匹配
