definitions:
  config:
    /hippo:configuration/hippo:modules/autoexport:
      /hippo:moduleconfig:
        autoexport:enabled: false
        autoexport:modules:
          operation: override
          type: string
          value: ['repository-data/application:/']
