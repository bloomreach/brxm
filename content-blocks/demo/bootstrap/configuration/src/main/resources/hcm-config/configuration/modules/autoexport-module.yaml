definitions:
  config:
    /hippo:configuration/hippo:modules/autoexport:
      /hippo:moduleconfig:
        autoexport:enabled:
          operation: override
          type: boolean
          value: false
        autoexport:modules:
          operation: override
          type: string
          value: ['bootstrap/content:/content', 'bootstrap/configuration:/']
