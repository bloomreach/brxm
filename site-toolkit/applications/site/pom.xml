<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.onehippo.ecm.hst</groupId>
    <artifactId>applications</artifactId>
    <version>2.03.05-SNAPSHOT</version>
  </parent>

  <name>Hippo Site Toolkit Container Web Application</name>
  <description>Hippo Site Toolkit Container Web Application</description>
  <groupId>org.onehippo.ecm.hst.applications</groupId>
  <artifactId>site</artifactId>
  <packaging>war</packaging>

  <dependencies>
    <dependency>
      <groupId>org.apache.geronimo.specs</groupId>
      <artifactId>geronimo-jta_1.1_spec</artifactId>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>javax.jcr</groupId>
      <artifactId>jcr</artifactId>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>javax.servlet</groupId>
      <artifactId>servlet-api</artifactId>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>javax.portlet</groupId>
      <artifactId>portlet-api</artifactId>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>javax.servlet</groupId>
      <artifactId>jstl</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>taglibs</groupId>
      <artifactId>standard</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>xml-apis</groupId>
      <artifactId>xml-apis</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>xerces</groupId>
      <artifactId>xercesImpl</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>xerces</groupId>
      <artifactId>xerces</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>xalan</groupId>
      <artifactId>xalan</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>javax.mail</groupId>
      <artifactId>mail</artifactId>
    </dependency>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-api</artifactId>
    </dependency>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-log4j12</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>log4j</groupId>
      <artifactId>log4j</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>commons-fileupload</groupId>
      <artifactId>commons-fileupload</artifactId>
    </dependency>
    <dependency>
      <groupId>commons-io</groupId>
      <artifactId>commons-io</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-core</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-webmvc</artifactId>
    </dependency>
    <dependency>
      <groupId>org.onehippo.ecm.hst</groupId>
      <artifactId>hst-api</artifactId>
      <version>${project.version}</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.onehippo.ecm.hst</groupId>
      <artifactId>hst-commons</artifactId>
      <version>${project.version}</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.onehippo.ecm.hst</groupId>
      <artifactId>hst-ocm</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>org.onehippo.ecm.hst.components</groupId>
      <artifactId>hst-core</artifactId>
      <version>${project.version}</version>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>org.onehippo.ecm.hst</groupId>
      <artifactId>hst-client</artifactId>
      <version>${project.version}</version>
    </dependency>
  </dependencies>

  <build>
    <defaultGoal>package</defaultGoal>
    <plugins>
      <plugin>
        <groupId>org.mortbay.jetty</groupId>
        <artifactId>maven-jetty-plugin</artifactId>
        <version>6.1.10</version>
        <configuration>
          <scanIntervalSeconds>5</scanIntervalSeconds>
          <webAppConfig>
            <contextPath>/site</contextPath>
            <tempDirectory>${project.build.directory}/work</tempDirectory>
          </webAppConfig>
          <connectors>
            <connector implementation="org.mortbay.jetty.nio.SelectChannelConnector">
              <port>8085</port>
            </connector>
          </connectors>
        </configuration>
        <dependencies>
          <!-- atomikos dependecies for running jetty:run[-war|-exploded] -->
          <dependency>
            <groupId>com.atomikos</groupId>
            <artifactId>transactions-api</artifactId>
            <version>3.1.4</version>
            <scope>runtime</scope>
          </dependency>
          <dependency>
            <groupId>com.atomikos</groupId>
            <artifactId>transactions-jta</artifactId>
            <version>3.1.4</version>
            <scope>runtime</scope>
          </dependency>
          <dependency>
            <groupId>com.atomikos</groupId>
            <artifactId>transactions</artifactId>
            <version>3.1.4</version>
            <scope>runtime</scope>
          </dependency>
          <dependency>
            <groupId>com.atomikos</groupId>
            <artifactId>atomikos-util</artifactId>
            <version>3.1.4</version>
            <scope>runtime</scope>
          </dependency>
          
          <!-- Provides a simple SLF4J for Jetty container itself.  -->
          <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <version>${slf4j.version}</version>
          </dependency>
          <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-simple</artifactId>
            <version>${slf4j.version}</version>
            <scope>runtime</scope>
          </dependency>
          
          <!-- Shared Libraries
          The followings will be located in shared lib folder in the servlet container. -->
          <dependency>
            <groupId>org.apache.geronimo.specs</groupId>
            <artifactId>geronimo-jta_1.1_spec</artifactId>
            <version>${jta.version}</version>
            <scope>runtime</scope>
          </dependency>
          <dependency>
            <groupId>javax.jcr</groupId>
            <artifactId>jcr</artifactId>
            <version>${javax.jcr.version}</version>
            <scope>runtime</scope>
          </dependency>
          <dependency>
            <groupId>org.onehippo.ecm.hst</groupId>
            <artifactId>hst-api</artifactId>
            <version>${project.version}</version>
            <scope>runtime</scope>
          </dependency>
          <dependency>
            <groupId>org.onehippo.ecm.hst</groupId>
            <artifactId>hst-commons</artifactId>
            <version>${project.version}</version>
            <scope>runtime</scope>
          </dependency>
          
        </dependencies>
      </plugin>
      <plugin>
        <inherited>true</inherited>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <target>1.5</target>
          <source>1.5</source>
        </configuration>
      </plugin>      
    </plugins>
  </build>
  
  <profiles>

    <profile>
      <id>tomcat</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>properties-maven-plugin</artifactId>
            <version>1.0-alpha-1</version>
            <executions>
              <execution>
                <phase>initialize</phase>
                <goals>
                  <goal>read-project-properties</goal>
                </goals>
                <configuration>
                  <quiet>true</quiet>
                  <files>
                    <file>../../build.properties</file>
                  </files>
                </configuration>
              </execution>
            </executions>
          </plugin>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-antrun-plugin</artifactId>
            <version>1.2</version>
            <executions>
              <execution>
                <id>deploy-to-tomcat</id>
                <phase>install</phase>
                <configuration>
                  <tasks>

                    <echo>Replacing HST Shared Libraries.</echo>
                    <copy file="${settings.localRepository}/javax/jcr/jcr/1.0/jcr-1.0.jar" tofile="${tomcat.hst.shared.lib}/jcr-1.0.jar" />
                    <copy file="${settings.localRepository}/org/apache/geronimo/specs/geronimo-jta_1.1_spec/1.1/geronimo-jta_1.1_spec-1.1.jar" tofile="${tomcat.hst.shared.lib}/geronimo-jta_1.1_spec-1.1.jar" />
                    <copy file="../../api/target/hst-api-${project.version}.jar" tofile="${tomcat.hst.shared.lib}/hst-api-${project.version}.jar" />
                    <copy file="../../commons/target/hst-commons-${project.version}.jar" tofile="${tomcat.hst.shared.lib}/hst-commons-${project.version}.jar" />
                    
                    <echo>Replacing cms web application into tomcat.</echo>
                    <delete dir="${tomcat.hst.cms.webapp}" />
                    <mkdir dir="${tomcat.hst.cms.webapp}" />
                    <unwar src="../../maven/archetype/target/gettingstarted/cms/target/gettingstarted-cms-1.01.00.war" dest="${tomcat.hst.cms.webapp}" />
                                      
                    <echo>Replacing site web application into tomcat.</echo>
                    <delete dir="${tomcat.server.home}/webapps/${project.artifactId}" />
                    <mkdir dir="${tomcat.server.home}/webapps/${project.artifactId}" />
                    <unwar src="${project.build.directory}/${project.artifactId}-${project.version}.war" dest="${tomcat.server.home}/webapps/${project.artifactId}" />
                    
                  </tasks>
                </configuration>
                <goals>
                  <goal>run</goal>
                </goals>
              </execution>
            </executions>
            <dependencies>
              <dependency>
                <groupId>javax.jcr</groupId>
                <artifactId>jcr</artifactId>
                <version>${javax.jcr.version}</version>
              </dependency>
              <dependency>
                <groupId>org.apache.geronimo.specs</groupId>
                <artifactId>geronimo-jta_1.1_spec</artifactId>
                <version>${jta.version}</version>
                <scope>runtime</scope>
              </dependency>
            </dependencies>
          </plugin>
        </plugins>
      </build>
    </profile>

    <profile>
      <id>jetspeed</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>properties-maven-plugin</artifactId>
            <version>1.0-alpha-1</version>
            <executions>
              <execution>
                <phase>initialize</phase>
                <goals>
                  <goal>read-project-properties</goal>
                </goals>
                <configuration>
                  <quiet>true</quiet>
                  <files>
                    <file>../../build.properties</file>
                  </files>
                </configuration>
              </execution>
            </executions>
          </plugin>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-antrun-plugin</artifactId>
            <version>1.2</version>
            <executions>
              <execution>
                <id>deploy-to-jetspeed</id>
                <phase>install</phase>
                <configuration>
                  <tasks>

                    <echo>Replacing HST Shared Libraries.</echo>
                    <copy file="${settings.localRepository}/javax/jcr/jcr/1.0/jcr-1.0.jar" tofile="${jetspeed.hst.shared.lib}/jcr-1.0.jar" />
                    <copy file="${settings.localRepository}/org/apache/geronimo/specs/geronimo-jta_1.1_spec/1.1/geronimo-jta_1.1_spec-1.1.jar" tofile="${jetspeed.hst.shared.lib}/geronimo-jta_1.1_spec-1.1.jar" />
                    <copy file="../../api/target/hst-api-${project.version}.jar" tofile="${jetspeed.hst.shared.lib}/hst-api-${project.version}.jar" />
                    <copy file="../../commons/target/hst-commons-${project.version}.jar" tofile="${jetspeed.hst.shared.lib}/hst-commons-${project.version}.jar" />
                    
                    <echo>Replacing cms web application into jetspeed.</echo>
                    <delete dir="${jetspeed.hst.cms.webapp}" />
                    <mkdir dir="${jetspeed.hst.cms.webapp}" />
                    <unwar src="../../maven/archetype/target/gettingstarted/cms/target/gettingstarted-cms-1.01.00.war" dest="${jetspeed.hst.cms.webapp}" />
                                      
                    <echo>Replacing site portlet application into jetspeed.</echo>
                    <delete dir="${jetspeed.server.home}/webapps/${project.artifactId}" />
                    <delete file="${jetspeed.server.home}/webapps/${project.artifactId}.war" />
                    <delete file="${jetspeed.portal.webapp}/WEB-INF/deploy/${project.artifactId}.war" />
                    <copy file="${project.build.directory}/${project.artifactId}-${project.version}.war" tofile="${jetspeed.portal.webapp}/WEB-INF/deploy/${project.artifactId}.war" />
                    
                    <echo>Copy core libaries into jetspeed</echo>
                    <copy todir="${jetspeed.portal.webapp}/WEB-INF/lib">
                      <fileset dir="${project.build.directory}/${project.artifactId}-${project.version}/WEB-INF/lib">
                        <include name="commons-proxy-*.jar" />
                      </fileset>
                      <fileset dir="${project.build.directory}/${project.artifactId}-${project.version}/WEB-INF/lib">
                        <exclude name="commons-*.jar" />
                        <exclude name="hst-client-*.jar" />
                        <exclude name="jstl-*.jar" />
                        <exclude name="slf4j-*.jar" />
                        <exclude name="log4j-*.jar" />
                        <exclude name="standard-*.jar" />
                        <exclude name="spring-*.jar" />
                        <exclude name="xml-apis-*.jar" />
                        <exclude name="xercesImpl-*.jar" />
                        <exclude name="xalan-*.jar" />
                        <exclude name="jdo2-*.jar" />
                        <exclude name="jpox-*.jar" />
                      </fileset>
                    </copy>

                    <echo>Copy hst configuration into jetspeed</echo>
                    <copy todir="${jetspeed.portal.webapp}/WEB-INF">
                      <fileset dir="${project.build.directory}/${project.artifactId}-${project.version}/WEB-INF">
                        <include name="hst*.properties" />
                        <include name="hst*.xml" />
                      </fileset>
                    </copy>
                    
                    <echo>Copy example psml page into jetspeed</echo>
                    <copy todir="${jetspeed.portal.webapp}/WEB-INF/pages">
                      <fileset dir="src/main/webapp/WEB-INF/portal-pages">
                        <include name="**/*.psml" />
                      </fileset>
                    </copy>
                                        
                    <echo><![CDATA[
========================================================================
[Notice]

Thank you for trying jetspeed profile.

Deploying artifacts are done, but you need to edit two web.xml files.

First, add the following servlet definition in /jetspeed/WEB-INF/web.xml

  <servlet>
    <servlet-name>HstSiteConfigServlet</servlet-name>
    <servlet-class>org.hippoecm.hst.site.container.HstSiteConfigServlet</servlet-class>
    <load-on-startup>1</load-on-startup>
  </servlet>
  

And, start the Jetspeed to auto-deploy site portlet application.
If the /site portlet application is deployed into your /webapps directory,
then stop the Jetspeed, 
and comment out the following servlet in /site/WEB-INF/web.xml:

  <!--
  <servlet>
    <servlet-name>HstSiteConfigServlet</servlet-name>
    <servlet-class>org.hippoecm.hst.site.container.HstSiteConfigServlet</servlet-class>
    <load-on-startup>1</load-on-startup>
  </servlet>
  -->

Finally, restart your Jetspeed and visit the following page:

http://localhost:8080/jetspeed/portal/

And, click `HST-2 Demo' menu!

========================================================================
                    ]]></echo>
                    
                  </tasks>
                </configuration>
                <goals>
                  <goal>run</goal>
                </goals>
              </execution>
            </executions>
            <dependencies>
              <dependency>
                <groupId>javax.jcr</groupId>
                <artifactId>jcr</artifactId>
                <version>${javax.jcr.version}</version>
              </dependency>
              <dependency>
                <groupId>org.apache.geronimo.specs</groupId>
                <artifactId>geronimo-jta_1.1_spec</artifactId>
                <version>${jta.version}</version>
                <scope>runtime</scope>
              </dependency>
            </dependencies>
          </plugin>
        </plugins>
      </build>
    </profile>
    
  </profiles>


</project>
