<?xml version="1.0" encoding="UTF-8"?>
<!--
  Copyright 2008 Hippo

  Licensed under the Apache License, Version 2.0 (the  "License");
  you may not use this file except in compliance with the License.
  You may obtain a copy of the License at

  http://www.apache.org/licenses/LICENSE-2.0

  Unless required by applicable law or agreed to in writing, software
  distributed under the License is distributed on an "AS IS"
  BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  See the License for the specific language governing permissions and
  limitations under the License.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.onehippo.ecm.hst</groupId>
    <artifactId>demosuite</artifactId>
    <version>2.03.08-SNAPSHOT</version>
  </parent>

  <name>Hippo Site Toolkit Demo Suite SITE Application</name>
  <description>Hippo Site Toolkit Demo Suite SITE Application</description>
  <groupId>org.onehippo.ecm.hst.demosuite</groupId>
  <artifactId>hst-demosuite-site</artifactId>
  <packaging>war</packaging>
  
  <properties>
    <site.log.dir.name>${site.webapp.root}/WEB-INF/logs</site.log.dir.name>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.apache.geronimo.specs</groupId>
      <artifactId>geronimo-jta_1.1_spec</artifactId>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>javax.jcr</groupId>
      <artifactId>jcr</artifactId>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>javax.servlet</groupId>
      <artifactId>servlet-api</artifactId>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>javax.portlet</groupId>
      <artifactId>portlet-api</artifactId>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>javax.servlet</groupId>
      <artifactId>jstl</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>taglibs</groupId>
      <artifactId>standard</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>xml-apis</groupId>
      <artifactId>xml-apis</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>xerces</groupId>
      <artifactId>xercesImpl</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>xerces</groupId>
      <artifactId>xerces</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>xalan</groupId>
      <artifactId>xalan</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>javax.mail</groupId>
      <artifactId>mail</artifactId>
    </dependency>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-api</artifactId>
    </dependency>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>jcl-over-slf4j</artifactId>
    </dependency>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-log4j12</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>log4j</groupId>
      <artifactId>log4j</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>commons-fileupload</groupId>
      <artifactId>commons-fileupload</artifactId>
    </dependency>
    <dependency>
      <groupId>commons-io</groupId>
      <artifactId>commons-io</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-core</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-webmvc</artifactId>
    </dependency>
    <dependency>
      <groupId>org.onehippo.ecm.hst</groupId>
      <artifactId>hst-api</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>org.onehippo.ecm.hst</groupId>
      <artifactId>hst-commons</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>org.onehippo.ecm.hst</groupId>
      <artifactId>hst-ocm</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>org.onehippo.ecm.hst.components</groupId>
      <artifactId>hst-core</artifactId>
      <version>${project.version}</version>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>org.onehippo.ecm.hst</groupId>
      <artifactId>hst-client</artifactId>
      <version>${project.version}</version>
    </dependency>
  </dependencies>

  <build>
    <defaultGoal>package</defaultGoal>
    <finalName>site</finalName>
    <plugins>
      <plugin>
        <groupId>org.mortbay.jetty</groupId>
        <artifactId>maven-jetty-plugin</artifactId>
        <version>6.1.18</version>
        <configuration>
          <scanIntervalSeconds>5</scanIntervalSeconds>
          <webAppConfig>
            <contextPath>/site</contextPath>
            <baseResource implementation="org.mortbay.resource.ResourceCollection">
              <resourcesAsCSV>src/main/webapp, ${project.build.directory}/${project.build.finalName}</resourcesAsCSV>
            </baseResource>
            <tempDirectory>${project.build.directory}/work</tempDirectory>
          </webAppConfig>
          <connectors>
            <connector implementation="org.mortbay.jetty.nio.SelectChannelConnector">
              <port>8085</port>
            </connector>
          </connectors>
        </configuration>
        <dependencies>
          <!-- atomikos dependecies for running jetty:run[-war|-exploded] -->
          <dependency>
            <groupId>com.atomikos</groupId>
            <artifactId>transactions-api</artifactId>
            <version>3.1.4</version>
            <scope>runtime</scope>
          </dependency>
          <dependency>
            <groupId>com.atomikos</groupId>
            <artifactId>transactions-jta</artifactId>
            <version>3.1.4</version>
            <scope>runtime</scope>
          </dependency>
          <dependency>
            <groupId>com.atomikos</groupId>
            <artifactId>transactions</artifactId>
            <version>3.1.4</version>
            <scope>runtime</scope>
          </dependency>
          <dependency>
            <groupId>com.atomikos</groupId>
            <artifactId>atomikos-util</artifactId>
            <version>3.1.4</version>
            <scope>runtime</scope>
          </dependency>
          
          <!-- Shared Libraries
          The followings will be located in shared lib folder in the servlet container. -->
          <dependency>
            <groupId>org.apache.geronimo.specs</groupId>
            <artifactId>geronimo-jta_1.1_spec</artifactId>
            <version>${jta.version}</version>
            <scope>runtime</scope>
          </dependency>
          <dependency>
            <groupId>javax.jcr</groupId>
            <artifactId>jcr</artifactId>
            <version>${javax.jcr.version}</version>
            <scope>runtime</scope>
          </dependency>
          
        </dependencies>
      </plugin>
      <plugin>
        <inherited>true</inherited>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <target>1.5</target>
          <source>1.5</source>
        </configuration>
      </plugin>
      
      <!-- Include DOJO Library -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <executions>
          <execution>
            <id>unpack</id>
            <phase>process-resources</phase>
            <goals>
              <goal>unpack</goal>
            </goals>
            <configuration>
              <artifactItems>
                <artifactItem>
                  <groupId>org.dojotoolkit</groupId>
                  <artifactId>dojo</artifactId>
                  <version>1.3.0</version>
                  <type>zip</type>
                  <overWrite>false</overWrite>
                  <outputDirectory>${project.build.directory}/${project.build.finalName}/javascript</outputDirectory>
                  <includes>**/*</includes>
                </artifactItem>
              </artifactItems>
            </configuration>
          </execution>
        </executions>
      </plugin>
      
    </plugins>
    
    <resources>
      <resource>
        <directory>src/main/resources</directory>
        <filtering>true</filtering>
      </resource>
    </resources>
    
  </build>
  
  <profiles>

    <profile>
      <id>tomcat</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>properties-maven-plugin</artifactId>
            <version>1.0-alpha-1</version>
            <executions>
              <execution>
                <phase>initialize</phase>
                <goals>
                  <goal>read-project-properties</goal>
                </goals>
                <configuration>
                  <quiet>true</quiet>
                  <files>
                    <file>../../build.properties</file>
                  </files>
                </configuration>
              </execution>
            </executions>
          </plugin>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-antrun-plugin</artifactId>
            <version>1.2</version>
            <executions>
              <execution>
                <id>deploy-to-tomcat</id>
                <phase>install</phase>
                <configuration>
                  <tasks>

                    <echo>Replacing HST Shared Libraries.</echo>
                    <copy file="${settings.localRepository}/javax/jcr/jcr/1.0/jcr-1.0.jar" tofile="${tomcat.hst.shared.lib}/jcr-1.0.jar" overwrite="true" />
                    <copy file="${settings.localRepository}/org/apache/geronimo/specs/geronimo-jta_1.1_spec/1.1/geronimo-jta_1.1_spec-1.1.jar" tofile="${tomcat.hst.shared.lib}/geronimo-jta_1.1_spec-1.1.jar" overwrite="true" />
                    
                    <echo>Replacing cms web application into tomcat.</echo>
                    <delete dir="${tomcat.hst.cms.webapp}" />
                    <mkdir dir="${tomcat.hst.cms.webapp}" />
                    <unwar src="../cms/target/cms.war" dest="${tomcat.hst.cms.webapp}" overwrite="true" />
                                      
                    <echo>Replacing site web application into tomcat.</echo>
                    <delete dir="${tomcat.server.home}/webapps/${project.build.finalName}.war" />
                    <delete dir="${tomcat.server.home}/webapps/${project.build.finalName}" />
                    <copy file="${project.build.directory}/${project.build.finalName}.war" tofile="${tomcat.server.home}/webapps/${project.build.finalName}.war" overwrite="true" />
                    
                  </tasks>
                </configuration>
                <goals>
                  <goal>run</goal>
                </goals>
              </execution>
            </executions>
            <dependencies>
              <dependency>
                <groupId>javax.jcr</groupId>
                <artifactId>jcr</artifactId>
                <version>${javax.jcr.version}</version>
              </dependency>
              <dependency>
                <groupId>org.apache.geronimo.specs</groupId>
                <artifactId>geronimo-jta_1.1_spec</artifactId>
                <version>${jta.version}</version>
                <scope>runtime</scope>
              </dependency>
            </dependencies>
          </plugin>
        </plugins>
      </build>
    </profile>

    <profile>
      <id>jetspeed</id>
      
      <properties>
        <site.log.dir.name>${org.apache.portals.logdir}</site.log.dir.name>
      </properties>

      <dependencies>
        <dependency>
          <groupId>org.onehippo.ecm.hst</groupId>
          <artifactId>hst-api</artifactId>
          <version>${project.version}</version>
          <scope>provided</scope>
        </dependency>
        <dependency>
          <groupId>org.onehippo.ecm.hst</groupId>
          <artifactId>hst-commons</artifactId>
          <version>${project.version}</version>
          <scope>provided</scope>
        </dependency>
        <dependency>
          <groupId>org.onehippo.ecm.hst.components</groupId>
          <artifactId>hst-core</artifactId>
          <version>${project.version}</version>
          <scope>test</scope>
        </dependency>
      </dependencies>
      
      <build>
        <plugins>
          <plugin>
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>properties-maven-plugin</artifactId>
            <version>1.0-alpha-1</version>
            <executions>
              <execution>
                <phase>initialize</phase>
                <goals>
                  <goal>read-project-properties</goal>
                </goals>
                <configuration>
                  <quiet>true</quiet>
                  <files>
                    <file>../../build.properties</file>
                  </files>
                </configuration>
              </execution>
            </executions>
          </plugin>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-war-plugin</artifactId>
            <version>2.0</version>
            <configuration>
              <webXml>src/main/webapp/WEB-INF/pa-web.xml</webXml>
            </configuration>
          </plugin>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-antrun-plugin</artifactId>
            <version>1.2</version>
            <executions>
              <execution>
                <id>deploy-to-jetspeed</id>
                <phase>install</phase>
                <configuration>
                  <tasks>

                    <echo>Replacing HST Shared Libraries.</echo>
                    <copy file="${settings.localRepository}/javax/jcr/jcr/1.0/jcr-1.0.jar" tofile="${jetspeed.hst.shared.lib}/jcr-1.0.jar" overwrite="true" />
                    <copy file="${settings.localRepository}/org/apache/geronimo/specs/geronimo-jta_1.1_spec/1.1/geronimo-jta_1.1_spec-1.1.jar" tofile="${jetspeed.hst.shared.lib}/geronimo-jta_1.1_spec-1.1.jar" overwrite="true" />
                    <copy file="../../api/target/hst-api-${project.version}.jar" tofile="${jetspeed.hst.shared.lib}/hst-api-${project.version}.jar" overwrite="true" />
                    <copy file="../../commons/target/hst-commons-${project.version}.jar" tofile="${jetspeed.hst.shared.lib}/hst-commons-${project.version}.jar" overwrite="true" />
                    
                    <echo>Replacing cms web application into jetspeed.</echo>
                    <delete dir="${jetspeed.hst.cms.webapp}" />
                    <mkdir dir="${jetspeed.hst.cms.webapp}" />
                    <unwar src="../cms/target/cms.war" dest="${jetspeed.hst.cms.webapp}" overwrite="true" />
                                      
                    <echo>Replacing site portlet application into jetspeed.</echo>
                    <delete dir="${jetspeed.server.home}/webapps/${project.build.finalName}" />
                    <delete file="${jetspeed.server.home}/webapps/${project.build.finalName}.war" />
                    <delete file="${jetspeed.portal.webapp}/WEB-INF/deploy/${project.build.finalName}.war" />
                    <copy file="${project.build.directory}/${project.build.finalName}.war" tofile="${jetspeed.portal.webapp}/WEB-INF/deploy/${project.build.finalName}.war" overwrite="true" />
                    
                    <echo>Copy core libaries into jetspeed</echo>
                    <copy todir="${jetspeed.portal.webapp}/WEB-INF/lib" overwrite="true">
                      <fileset dir="${project.build.directory}/${project.build.finalName}/WEB-INF/lib">
                        <include name="commons-proxy-*.jar" />
                      </fileset>
                      <fileset dir="${project.build.directory}/${project.build.finalName}/WEB-INF/lib">
                        <exclude name="commons-*.jar" />
                        <exclude name="hst-client-*.jar" />
                        <exclude name="jstl-*.jar" />
                        <exclude name="slf4j-*.jar" />
                        <exclude name="log4j-*.jar" />
                        <exclude name="standard-*.jar" />
                        <exclude name="spring-*.jar" />
                        <exclude name="xml-apis-*.jar" />
                        <exclude name="xercesImpl-*.jar" />
                        <exclude name="xalan-*.jar" />
                        <exclude name="jdo2-*.jar" />
                        <exclude name="jpox-*.jar" />
                        <exclude name="wicket-*.jar" />
                      </fileset>
                    </copy>

                    <echo>Copy hst configuration into jetspeed</echo>
                    <copy todir="${jetspeed.portal.webapp}/WEB-INF" overwrite="true">
                      <fileset dir="${project.build.directory}/${project.build.finalName}/WEB-INF">
                        <include name="hst*.properties" />
                        <include name="hst*.xml" />
                      </fileset>
                    </copy>
                    
                    <echo>Copy example psml page into jetspeed</echo>
                    <copy todir="${jetspeed.portal.webapp}/WEB-INF/pages" overwrite="true">
                      <fileset dir="src/main/webapp/WEB-INF/portal-pages">
                        <include name="**/*.psml" />
                      </fileset>
                    </copy>
                                        
                    <echo><![CDATA[
========================================================================
[Notice]

Thank you for trying jetspeed profile.

Deploying artifacts are done, but you need to the web.xml file in jetspeed.

Add the following servlet definition in /jetspeed/WEB-INF/web.xml

  <servlet>
    <servlet-name>HstSiteConfigServlet</servlet-name>
    <servlet-class>org.hippoecm.hst.site.container.HstSiteConfigServlet</servlet-class>
    <init-param>
      <param-name>forceful.reinit</param-name>
      <param-value>true</param-value>
    </init-param>
    <load-on-startup>1</load-on-startup>
  </servlet>
  
Restart your Jetspeed and visit the following page:

http://localhost:8080/jetspeed/portal/

And, click `HST-2 Demo' menu!

========================================================================
                    ]]></echo>
                    
                  </tasks>
                </configuration>
                <goals>
                  <goal>run</goal>
                </goals>
              </execution>
            </executions>
            <dependencies>
              <dependency>
                <groupId>javax.jcr</groupId>
                <artifactId>jcr</artifactId>
                <version>${javax.jcr.version}</version>
              </dependency>
              <dependency>
                <groupId>org.apache.geronimo.specs</groupId>
                <artifactId>geronimo-jta_1.1_spec</artifactId>
                <version>${jta.version}</version>
                <scope>runtime</scope>
              </dependency>
            </dependencies>
          </plugin>
        </plugins>
      </build>
    </profile>
    
  </profiles>


</project>
